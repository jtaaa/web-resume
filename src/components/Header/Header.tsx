import React from 'react';
import { HeaderContainer } from './elements';

const Header = () => {
  return (
    <HeaderContainer>
      <h1>Joshua Allum</h1>
    </HeaderContainer>
  );
};

export default Header;
