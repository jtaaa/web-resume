import styled from 'styled-components/macro';
import Box from 'components/Box';

const Body = styled(Box)``;

Body.defaultProps = {
  fontSize: [2, 3, 4],
  lineHeight: [2, 3, 4],
};

export default Body;
